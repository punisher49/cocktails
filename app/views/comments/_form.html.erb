<%= form_for([@cocktail,@cocktail.comments.build]) do |f|%>
  <div>
  <p>
  <%= f.label :name %>
  <%= f.text_field :name %>
  </p>
  </div>
<div>
  <p>
  <%= f.label :body, 'Comment' %> <br>
  <%= f.text_area :body %>
  </p>
  <br>
  <p>
      <%= f.submit %>
  </p>
  </div>
<% end %>
