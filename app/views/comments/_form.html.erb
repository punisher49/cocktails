<%= form_for([@cocktail,@cocktail.comments.build]) do |f|%>
  <div class="text_optional">
  <p>
  <%= f.label :name %>
  <%= f.text_field :name %>
  </p>
  </div>
<div class="text_optional">
  <p>
  <%= f.label :body, 'Comment' %> <br>
  <%= f.text_area :body %>
  </p>
  <br>
  <p>
      <%= f.submit %>
  </p>
  </div>
<% end %>
